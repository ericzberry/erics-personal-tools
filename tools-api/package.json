{
  "name": "erics-tools-api",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../mobile-app run build",
    "predeploy": "npm --prefix ../mobile-app run build",
    "test": "node --test tests/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {"wrangler": "^4.0.0"}
}
